# Final empirical section

## Clean uncapped rerun protocol — 23 September 2026

The current branch is `empirics-uncapped-20260923`. All previously generated
figures under `results/` have been removed before the new run.

Headline portfolio evaluation is now fully uncapped. The response-one
validation criterion, OOS returns, complexity diagnostics, and reported
portfolio weights therefore refer to the same estimator. Gross exposure and
maximum stock weights are retained as diagnostics rather than mechanically
truncated.

Finite nonlinear representations use 2,000 final coordinates. Every random
representation is run with exactly two fixed seeds, 0 and 1. Seed 0 is the
headline path and seed 1 is a robustness replication. The linear model is
deterministic and is run once.

For Gaussian and Matérn representations the initial lengthscale search is much
denser than before: a 41-point logarithmic grid on
([0.125,4]	imesell_{median}), augmented to contain exactly the anchor
multipliers
[
0.125,;0.25,;0.5,;0.75,;1,;1.5,;2,;4.
]
Thus the search has well over 30 candidate lengthscales and cannot select the
old lower boundary merely because the grid stopped at 0.5.

For each candidate lengthscale, ridge tuning uses 120 complexity-spaced
lambda values. Lengthscale and lambda are selected chronologically by the
response-one validation loss.

The clean post-run diagnostics include:

- lambda, effective complexity C, and C/T through expanding history;
- annual return, Sharpe, volatility, and drawdown versus effective complexity;
- uncapped gross exposure versus complexity;
- a paired annual-block-bootstrap comparison of annual lambda re-optimization
  against one initial calibration followed by the Gaussian 1/T rule;
- two-seed performance, loss, leverage, and monthly-return stability.

No legacy r=1.5 lambda-scaling figures are regenerated in this clean run.

This file records the empirical design that matches the current paper. It also
marks which older figures must not be used in the main text.

## Core empirical message

The empirical section is organized around one mechanism:

more history -> less shrinkage -> more active portfolio directions,
while effective complexity relative to available history declines.

For the Gaussian representation the paper's fast-decay benchmark implies the
inverse-history schedule

\[
\lambda_T = \lambda_0 T_0 / T.
\]

There is no r parameter in this Gaussian experiment.

## Main-text order

### A. Data and chronological design

Keep this short.

- U.S. JKP stock-level panel.
- 132 characteristics.
- Monthly cross-sectional rank normalization to [-0.5, 0.5].
- Initial train: 1963-1972.
- Initial validation: 1973-1977.
- OOS test formation years: 1978-2024.
- Annual refits; stock weights change monthly with characteristics.
- The learnability experiment below always estimates the operator and the
  portfolio coefficients on the entire expanding history available before the
  test year.

### B. Portfolio performance across representations

For the finite numerical implementations, use a common nominal nonlinear
feature dimension of 2,000 coordinates. Gaussian and all Matérn specifications
use 2,000 random Fourier features. The NTK uses 1,000 Monte Carlo hidden
directions, each contributing an NNGP/ReLU coordinate and a derivative
coordinate, for exactly 2,000 final NTK feature coordinates. This makes the
nominal-size comparison across nonlinear representations like-for-like. The
linear specification has 133 coordinates including the constant.

Moving the stationary kernels from 1,000 to 2,000 random features invalidates
their previously saved backtests, complexity curves, spectra, and performance
table rows. NTK remains at 2,000 final coordinates, but is regenerated under
the unified convention. Rerun every nonlinear kernel before updating the
manuscript.

Main table:

- Linear
- Gaussian
- NTK
- Matérn 3/2

Move Matérn 1/2 and Matérn 5/2 to the appendix.

Keep the equity figure and the performance table. The interpretation is not a
kernel horse race. Similar OOS Sharpe ratios across substantially different
representations are evidence that nominal representation size alone does not
order finite-sample portfolio quality.

### C. Managed-portfolio spectrum

Main figure generated by final_spectra.py:

- Linear
- NTK
- Matérn 3/2

Use raw eigenvalues; do not normalize by the leading eigenvalue.

For Matérn 3/2 report the interior-spectrum estimate of b in the figure or
caption, but do not draw the fitted power-law line. The pre-specified fit
window is ranks 5-450.

Gaussian, Matérn 1/2, and Matérn 5/2 spectra go to the appendix. Gaussian is
described as the fast-decay benchmark, b=infinity, rather than being assigned a
finite power-law exponent.

Suggested caption:

"Managed-portfolio spectra. Ordered empirical eigenvalues of the expanding
managed-payoff second-moment operator in the final estimation window. Values
are shown in their original scale. For the Matérn 3/2 representation, the
reported spectral exponent is estimated on ranks 5-450; the fitted line is
omitted from the figure. Gaussian and additional Matérn specifications are
reported in the appendix."

### D. History, shrinkage, and effective complexity

This is the central empirical learnability figure.

Run:

python expanding_window_gaussian.py --characteristics all

The script calibrates lambda_0 once on the initial train/validation split and
then compares:

1. fixed initial lambda_0;
2. lambda_T = lambda_0 T_0 / T;
3. the original annual-validation rule as an appendix benchmark.

At every test year:

- T is the number of monthly observations in the full expanding refit sample;
- Sigma-hat_T is estimated from all observations before that year;
- C_T is computed from the eigenvalues of that expanding operator;
- the portfolio coefficients are refitted on the same expanding history.

Main figure:

Panel A: T -> lambda_T
Panel B: T -> C_T
Panel C: T -> C_T / T

For Panels B and C, show only the five-year centered moving average in the
main text. The exact raw annual series is saved and shown in the appendix.
The lambda schedules are exact and are not smoothed.

Main figure includes only fixed lambda and the inverse-history rule. Annual
validation is an appendix benchmark because its year-to-year tuning noise
obscures the economic mechanism.

Suggested caption:

"History, shrinkage, and learnable portfolio complexity. The Gaussian penalty
is calibrated once on the initial sample. The solid path applies the
inverse-history rule lambda_T = lambda_0 T_0/T; the dashed path holds the
initial penalty fixed. At every annual refit, the managed-payoff operator and
portfolio coefficients are re-estimated using all prior observations. The
middle and lower panels show five-year centered moving averages of the annual
effective-complexity estimates; unsmoothed values are reported in the
appendix. Declining shrinkage activates additional managed-portfolio
directions as history accumulates, while C_T/T measures complexity relative to
the amount of available return history."

### D.1 Matérn scaling from the economic spectrum

Run:

python expanding_window_matern.py --characteristics all

For each of Matérn 1/2, 3/2, and 5/2, calibrate the initial penalty once and
estimate the economically relevant spectral exponent directly from the
managed-payoff spectrum using only pre-OOS information through 1977. Freeze
that estimate before the first test year. Under the current r=1 theorem the
headline rule is

\[
\widehat\alpha
=
\frac{\widehat b}{\widehat b+1},
\qquad
\lambda_T
=
\lambda_0
(T/T_0)^{-\widehat\alpha}.
\]

The operational \(\widehat b\) is estimated on an interior pre-OOS spectral
window: ranks 5 through
\(\min\{450,\lfloor 0.8J\rfloor\}\), where \(J\) is the number of active
positive eigenvalues. The estimate is frozen. Annual expanding-window
estimates of \(b\) are retained only as diagnostics and are never used to tune
the OOS penalty.

The Sobolev/Matérn relation

\[
b_{\mathrm{theory}}=\frac{2s}{D},
\qquad
s=D/2+\nu,
\]

is retained as an appendix benchmark rather than imposed on the economic
spectrum. This follows the main paper's principle that the managed-payoff
spectrum, not the formal kernel alone, is the economically relevant object.

Retain all analysis objects under:

results/expanding_matern/all/

including:

- annual_diagnostics.parquet and CSV
- monthly_returns.parquet
- annual_spectra.parquet
- one pre-OOS spectrum per Matérn kernel
- one annual spectral-fit file per Matérn kernel
- one initial calibration profile per Matérn kernel
- fitted annual coefficient arrays for every rule
- calibration_and_rates.csv
- performance_summary.csv
- raw annual appendix plots
- smoothed main-text plots

The main Matérn figure compares fixed lambda with the frozen empirical-b
schedule. The s-based schedule and annual validation are robustness
benchmarks.

### E. Performance of the shrinkage schedules

Use performance_summary.csv generated by expanding_window_gaussian.py.

Columns:

- OOS Sharpe
- response-one quadratic criterion
- mean C
- mean C/T

Headline comparison:

- annual validation
- fixed initial penalty
- initial calibration + 1/T

Do not label the 1/T rule with r=1.5 or any other r.

Suggested table note:

"All schedules use the same Gaussian representation and the same expanding
estimation windows. The initial penalty and lengthscale are selected before
the OOS period. Under the inverse-history rule only the deterministic
sample-size adjustment changes after the initial calibration. Performance is
evaluated on the common uncapped monthly payoff series."

## Output schema

The central annual file is:

results/expanding_gaussian/all/annual_diagnostics.parquet

One row per test year and rule.

Columns:

- test_year
- T
- rule
- lambda
- C
- C_over_T
- annual_oos_loss
- annual_oos_sharpe
- C_ma5
- C_over_T_ma5
- annual_oos_loss_ma5
- annual_oos_sharpe_ma5

Monthly payoff file:

results/expanding_gaussian/all/monthly_returns.parquet

Summary table:

results/expanding_gaussian/all/performance_summary.csv

## Main figures

1. Existing OOS equity paths across the four headline representations.
2. results/final_spectra/main_spectra.png
3. results/expanding_gaussian/all/gaussian_expanding_main.png

That is enough for the main empirical narrative.

## Appendix figures

- all individual spectrum figures from results/final_spectra/
- raw annual expanding-window diagnostics
- annual-validation benchmark
- Matérn 1/2 and Matérn 5/2 performance
- any sensitivity of the Matérn b fit window

## Figures to retire from the main text

Do not use the old Gaussian paper-scaling figures based on r=1.5.

In particular, figures under results/lambda_scaling/gaussian that describe
"Paper scaling (r = 1.5)" are legacy diagnostics and are inconsistent with the
current Gaussian interpretation of the paper.

Do not use PNG-reconstructed or manually smoothed curves as empirical
evidence. All final figures must be generated from the saved numerical annual
or monthly output files.
